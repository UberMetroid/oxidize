[workspace]
members = [
    "oxidize-engine",
    "oxidize-server",
    "oxidize-ui"
]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"

[profile.release]
lto = true
opt-level = 'z'
codegen-units = 1
panic = 'abort'
strip = true
